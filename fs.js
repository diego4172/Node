const fs = require('fs')

/*fs.readFile('index.html','utf8',(err,content)=>{
    if(err){
        console.log(err)
    }else{
        console.log(content)
    }
})

fs.rename('index.html','main.html',err=>{
    if(err){
        throw err;
    }
    console.log('nombre cambiado correctamente');
})

fs.appendFile('index.html','<p>nuevo contenido</p>',err=>{
    if(err){
        throw err;
    }
    console.log('contenido agregado')
})
fs.writeFile('index.html','<html></html>',err=>{
    if(err){
        throw err;
    }
    console.log('contenido reempplazada exitosamente')
})
fs.unlink('prueba.html',err=>{
    if(err){
        throw err;
    }
    console.log('contenido eliminado exitosamente')
})*/
